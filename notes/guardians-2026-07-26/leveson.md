# Leveson seat report (verbatim, received 2026-07-26 00:24)

LEVESON SEAT — SYSTEM SAFETY REPORT (guardians 2026-07-26)

## 1. CORE JUDGMENT

The low-level safety loops in this system are excellent and largely closed; the top-level control loop — the one that runs through the operator — is the missing structure, and the operator's felt discomfort is the accurate perception of that gap.

What is profound: the provenance chain (dated packs under artifacts/live/, per-call JSONL, errata-on-top, packets with byte-identical fusion) is genuine safety infrastructure, and it demonstrably works. When the JCB board was extended on 07-18, the superseded verdict was preserved under an erratum on top of artifacts/live/kimi-k3-bench-2026-07-18/README.md, per-model JSONL was committed (k26/fable5/terra/sol/luna/opus46 reports), and I verified the live site's 13 DATA rows (site/index.html:518-530) trace to that pack — the #1–#2 gap of 0.014 in the prose matches the data, the "13 models" denominator matches the row count. Unacceptable-loss (1) — site asserting numbers its evidence no longer supports — has NOT occurred. The control preventing it, however, is a one-time manual assertion at inlining time (docs/PUBLIC_BENCH.md: "ranking asserted equal to the pack README before inlining"), not a standing mechanism; it will hold exactly as long as every future deploy is done by an agent that re-reads that sentence.

Also profound and under-noticed as a SAFETY result: Manifund P2 silently satisfied the deepest doctrinal demand of the 07-09 red-team. Doctrine Finding 1 ("no pack whose reference column was produced by neither the operator nor the stack"; 30-day test, 0/28 packs at 07-09) is now passed — ACX 2024 grant decisions and EA Community Choice outcomes are external references the stack didn't produce and the operator didn't write, with predictions registered before unblinding (commits ca1928c, 892e0c0, c0f5cd9). The validation loop DID open. Nobody connected that back to the finding; the ledger has no closure state, so a passed test looks identical to an ignored one.

What is churn: generating further operator-adjudication items while the existing operator queue has zero measured throughput. Hard evidence: of the six doctrine edits the 07-09 panel proposed to the operator (§8 external-reference clause, §9 slogan, §1 metric sentence, §4 retitle, §10 retirement criterion, cost sig-figs), zero are in docs/PRINCIPLES.md 17 days later (last touched 2026-07-14 by an unrelated commit; §1's "self-refutation count", §9's "Distribution > capability", and §10's closing sentence all still verbatim). Meanwhile every agent-executable finding from the same panel closed: fuse() idempotency shipped same-day, #51's ordered-probit is in CHANGELOG [Unreleased], the eps/gamma confidence-knob gate fired and the knobs were deleted. The system closes loops agents can execute at machine speed and stalls indefinitely on loops only the human controller can close — so the operator-owned backlog grows monotonically. That asymmetry, not work volume per se, is the churn generator. Reflexively: this panel's own output lands in the same unbounded queue unless the queue mechanism exists.

## 2. STRONGEST OBJECTION TO THE CURRENT TRAJECTORY

The hazardous state is not "too many findings"; it is unbounded operator-side WIP with no feedback signal. Nothing tells the operator (a) which proposed-to-operator items are still open, (b) which pre-registered 30-day tests (dated 2026-08-08, sitting only inside notes/red-team-2026-07-09/doctrine.md with no owner or schedule) are due, or (c) that a standing external commitment — PUBLIC_BENCH's "we commit to same-week runs on every frontier release" — exists with no watch mechanism, so a missed release stales the public surface silently. Every additional instrument, wave, or panel adds to the claim surface that this one controller must audit, while adding zero audit capacity. Under the current structure, unacceptable-loss (2) — meaning drowning in churn — is not a risk; it is the steady-state output of the design. The 07-09 standing finding ("the validation loop closes through one person; distribution is the unlisted binding constraint") already named this; it stands, and P2 is the one partial exception.

## 3. MOVES (≤3, each with its loser condition)

MOVE 1 — Close the 07-09 ledger and cap the operator queue. One file (e.g. notes/red-team-2026-07-09/ADJUDICATION-STATUS.md or a repo-level OPERATOR-QUEUE.md, pointed to from AGENTS.md so sessions actually load it): every proposed-to-operator item, deferred gate, and 30-day test, each with state and check date. Record that P2 passed doctrine Finding 1's test. Cap open operator-decision items (~5); any process — guardian panels included — wanting to add one beyond the cap must first get one closed or explicitly dropped. This is back-pressure, the one control the system lacks. Sized: one session, $0. Loser condition: if the file is not wired into session-start context, it becomes artifact number 41 in notes/ and the move was churn.

MOVE 2 — Make the site-pack bind mechanical at the deploy boundary. Deploy is currently bare scp (PUBLIC_BENCH § Hosting). Replace with a deploy script whose precondition parses the DATA block out of site/index.html and diffs it against the pack's committed leaderboard JSONs, refusing to ship on mismatch; the script is the only sanctioned deploy path. $0, offline, no new test suite (it is a deploy gate, not a test — but per repo law, operator approval before building it). Sized: ~half a session. Loser condition: if board updates stay rare (~monthly) and single-sourced, a manual diff ritual at deploy time carries the same assurance and the script is bloat — the decider is whether "same-week coverage of every frontier release" is actually practiced; if that commitment is real, the gate pays for itself on the first hurried release-day deploy.

MOVE 3 — Adopt the retirement criterion and generalize the admission gate. The 07-09 §10 edit ("an instrument no claim has needed in two versions is a retirement candidate") is written, word-counted, and waiting; adopting it is a one-line operator decision. Pair it with the pattern that already works: retirements in this repo succeed exactly when the gate was named ex ante (canonical_v2_attr_first retired via a pre-committed sweep in 0.2.0; eps/gamma knobs deleted when their pre-named $0 fit came back flat), and the axis-research wave admitted only 6 of 69 candidates because a pre-registered rubric plus planted decoys existed before scoring. Constraint form: no new instrument or axis without a pre-registered admission rubric and a named retirement condition at birth. Sized: doctrine edit plus one paragraph in PRINCIPLES §10. Loser condition: if in two board versions nothing is ever retired under the criterion, the criterion is decoration and the real problem is that "version" cadence is too slow to ever trigger it — then the gate should be denominated in claims, not versions.

## 4. SOUND — MUST NOT BE CHURNED

- The errata-on-top / dated-pack / committed-JSONL provenance discipline, including the 07-18 board-extension handling. This is the strongest control in the system and the reason loss (1) hasn't occurred. Do not "improve" it into a heavier process.
- The scripted-pathology admission gate (PRINCIPLES §2) and its organic generalization in axis-research wave 2 (planted decoys, 6/69 admitted, WEAK verdicts diagnosed as decoy-design failure rather than quietly passed). This is a working volume-bounding mechanism that emerged without doctrine forcing it.
- The Manifund P2 pre-registration flow (predictions committed before unblinding, post-registration fix quoted honestly). It is the repo's only externally-referenced validation loop; it should be named as the template for every future map claim, not rebuilt.
- Packet fusion idempotency and its planted-test validation (adjudicated and executed 07-09; cite, don't reopen).
- The adjudicated 07-09 findings themselves — the convergence map and survival list are a finished audit. Re-red-teaming the doctrine before the existing edits are adjudicated would be the purest possible form of the churn this panel was convened about.

## 5. FORESEEN CONFLICT

With Hamming: that seat will likely propose the next ambitious result (axis wave 3, the full-corpus map, a new tier) as the significance play. My constraint is a sequencing veto, not a content veto: every new result enlarges the claim surface audited by a single controller whose queue already has zero throughput, so Moves 1 and 3 (one session, ~$0) come before any new instrument tranche. If Hamming's answer is "distribution/external contact IS the next problem," we converge — that is the 07-09 standing finding, and P2 is the evidence it pays. Partial conflict with Feynman: wave-2 axis results (scar_tissue_density inversion, eschatological_seriousness) deserve the noticing Feynman will give them, but they are pre-publication claims that will eventually land on the public surface and must enter through the same pack-binding gate as the board — amplification before that binding exists recreates the hazard the site has so far avoided. With Dijkstra I expect agreement with an ordering nuance: canonicalizing sort_texts/judgement-run.v1 for cross-repo use is safe precisely because there are zero external consumers today — pinning a versioned contract plus conformance check now, before the first consumer, is the cheapest moment in the artifact's entire life to do it; the hazard (loss 4, silent contract drift under consumers) only becomes live after promotion, so the contract must ship with, not after, the promotion.
