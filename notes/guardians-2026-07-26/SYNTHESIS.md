# Guardian Panel Synthesis — 2026-07-26

Four independent fable seats (feynman.md, dijkstra.md, leveson.md, hamming.md
verbatim in this dir; BRIEF.md is the question). Synthesis written only after
all four reports; lead fact-checked the two load-bearing claims before
writing (acx2024-explain.json weights match Feynman's citation exactly;
"Distribution > capability" verbatim at PRINCIPLES.md:84 confirms Leveson's
zero-of-six-edits-landed claim).

## Independent convergence (no collusion; treat as strong evidence)

All four seats, from four disciplines, reached the same core verdict:

**The profound thing already exists and is under-noticed; the churn is
structural, not a quality problem.** Specifically:

1. **The P2 mechanism inversion is the repo's one real finding** (Feynman:
   "the one real finding in the portfolio," verified to the JSON; Hamming:
   "#1 by a wide margin... the profound result already exists and is
   under-noticed — by everyone but her"). The juried order professes EV/$
   and reveals 0.000 weight on it (epistemic integrity 0.794); the crowd
   order actually follows EV/$. Pre-registered, twice-measured, $1.43.
2. **The churn generator is a one-mind loop.** Hamming: the validation loop
   still contains one mind, 17 days after the 07-09 adjudication ordered
   external contact as move #1 — every shipment since has been supply-side.
   Leveson, independently, mechanized the same diagnosis: agent-executable
   loops close at machine speed; operator-owned loops have zero measured
   throughput (0/6 doctrine edits landed in 17 days), so the operator queue
   grows monotonically. The discomfort is accurate perception, not mood.
3. **The evidence discipline is the moat — do not churn it.** All four
   independently listed pre-registration, dated packs, errata-on-top,
   denominators, counterbalance/flip reporting as inviolable. Dijkstra adds
   `src/packet.rs` as the one book-quality object (bitwise-fusible sufficient
   statistic), and sort.rs as the model convenience surface.
4. **Under-noticed closures:** Leveson caught that P2 *silently passed* the
   07-09 doctrine's hardest test (external reference column, 0/28 packs at
   the time) and nobody marked the loop closed. Noticing of exactly the kind
   the operator asked for.

## Named conflicts and resolutions

**C1. Truth vs importance — the publication bar for P2 (Feynman vs Hamming).**
Feynman wants second judge + polish-control attribute + cohort deconfound;
Hamming says indefinite bar-raising is the immune response wearing rigor's
clothes. Resolution: the second judge + polish control (~$0.35–$2, Feynman
Move 2) DOES gate the post — cheap, decisive, strengthens or kills. The
cohort deconfound (Feynman Move 1) does NOT gate — the confound is quoted
honestly in the post and the within-cohort test is the pre-registered P3
follow-up. This satisfies both disciplines: doctrine permits publishing with
the stated noise class; contact stops being deferred.

**C2. Correctness vs importance — canonicalization timing (Dijkstra vs
Hamming).** Dijkstra: the doorway (21 verbs, four formats) hides the
book-quality kernel; minimal surface is a precondition of distribution.
Hamming: don't canonicalize ahead of demand; the first consumer seam
(exopriors-core /v1/judgements → cardinald) already exists and pulls.
Resolution: Dijkstra Move 1 (partition by decree — canonical = sort_texts +
CLI sort/judge + packet format; everything else labeled research-grade) is
docs-only, zero churn, do it now. Packet-loop closure (--packets-out on sort
+ `cardinal fuse` verb) and reject-not-clamp hardening are queued BEHIND the
contact moves; Move 2's own loser condition is Hamming's thesis, so it waits
for a pulling consumer. Leveson's ordering nuance adopted: whenever a surface
IS promoted, the versioned contract ships WITH the promotion (pre-consumer is
the cheapest moment in the artifact's life).

**C3. Safety vs importance — the spice (Hamming anticipating Leveson).**
Publishing P2 publicly characterizes a named community's (and implicitly a
named judge's) funding decisions. Mitigation is real: the crowd-vs-juried
symmetry makes neither mechanism the villain, and the finding is framed as a
mechanism property. But whether to publish, where, and under what framing is
irreducibly the operator's call — handed to her, not resolved here. Fallback
if declined: JCB post standalone, next corpus less personal (at stated cost
in discourse-adjacency). Related Leveson constraint adopted: wave-2 axis
results are NOT amplified publicly before they enter through the same
pack-binding gate as the board.

**C4. Sequencing veto (Leveson, unopposed).** Before any new instrument
tranche: close the 07-09 ledger (one file recording each proposed-to-operator
item's state, including "P2 passed doctrine Finding 1's test"), cap open
operator-decision items (~5), wire it into AGENTS.md. One session, $0. Every
seat's plan is compatible; no seat objected. This panel's own output enters
that queue under the cap — Hamming's warning that panels are themselves
immune-response churn unless the move executes is accepted as a design
constraint on this document: it hands over three decisions and stops.

## The sequence (proposed next tranche, in order)

1. **Operator queue ledger + cap** (Leveson M1; one session, $0).
2. **Second judge + polish control on ACX explain fit** (Feynman M2;
   ~$0.35–$2, hard stop $2). Outcome either arms the post or kills it.
3. **Draft the P2 public post** (Hamming M1; mechanism-property framing,
   noise class and cohort confound stated, evidence pack + pairwiseratio.org
   linked; merged with the 07-09 JCB external-contact probe). DRAFT ONLY —
   publication is the operator's decision (C3).
4. **Docs-only canonical partition** (Dijkstra M1).
5. **Gate axis wave 3 behind one arrest event** (Hamming M3 + Feynman M3:
   no wave 3 before (a) one stranger sees one admitted-axis top-10, and
   (b) the small-tier replication + test-retest baseline runs on the frozen
   wave-2 probe sets, ~$2–4). If strangers keep getting arrested, the axis
   program is vindicated as the public surface's content strategy and
   resumes with elevated priority.

STOP list (explicit): axis wave 3 (until gated), cardinald extension
(premature deployment carrying one consumer's schema — Dijkstra), new
instruments/axes without pre-registered admission rubric + named retirement
condition at birth (Leveson M3), further strategy panels/red-teams until the
07-09 ledger has throughput (Hamming, Leveson, unanimous).

## Handed to the operator (the only three decisions)

1. **Publish P2?** After the second-judge gate: yes/no, venue, and the
   spice framing (C3). Everything up to the draft is agent-executable.
2. **Adjudicate the six 07-09 doctrine edits** (they are written; each is a
   one-line accept/reject; the ledger file will list them).
3. **Adopt the retirement criterion + admission-gate constraint** (Leveson
   M3 — one-line decision, the text exists).
