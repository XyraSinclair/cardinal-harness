# Slate, live: a raided skill through the intake front door (2026-07-07)

`cardinal slate @item --note "..."`: item + collector's voice-note ramble
→ model names the stakeholders whose judgment matters → each proposes
attributes blind to the others → one merge pass (rewordings only).
Breadth (independent perspectives converging) is the measured proxy for
"alive in social reality". Item: the probe-integrity skill description;
note: a verbatim-style ramble. 6 calls, $0.0026.

Auto-named stakeholders: DevOps engineers · Security auditors · Site
reliability teams · Automation platform users.

Top of the slate: **false clean signal resistance [3/4 backers]** —
three unlike perspectives independently converged on the skill's actual
load-bearing property. 11 entries total, breadth-sorted.

Honest notes: (1) the merge deliberately errs toward UNDER-merging
(near-duplicates like "probe-result correctness"/"probe result validity"
survive) — downstream `canonize` merges by measurement, and losing a
dimension at intake is the unrecoverable error; (2) json_object response
mode forbids top-level arrays — three parse failures during bring-up
traced to the model contorting around that constraint; prompts now ask
for wrapped shapes natively. The slate is hypotheses at pennies; the
handoff is `canonize` (transmissibility), `sort`/`distinguish`
(measurement), `explain` (inverse mode: believed ranking → attributes).
