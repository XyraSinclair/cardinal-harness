# AGENTS.md

**ARCHIVED.** Everything moved to
[llmsort](https://github.com/XyraSinclair/llmsort) on 2026-08-19:

- the crate (root package, crates.io `llmsort`);
- the living research code (llmsort `experiments/`);
- the record — evidence packs, notes, campaigns, batteries, data, python
  analysis (llmsort `research/`), `PROGRAM.md` at its root;
- the public sites live in exopriors-core `sites/`.

This repo exists only for pre-extraction git history (the
`cardinal-harness` → `ratiometer` → `llmsorting` era). Nothing new lands
here. Do not resurrect code or commit evidence here — it all belongs in
llmsort.
