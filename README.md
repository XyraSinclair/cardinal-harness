# llmsort-lab (archived)

Everything is **[llmsort](https://github.com/XyraSinclair/llmsort)** now
— the crate (`cargo install llmsort`,
[crates.io](https://crates.io/crates/llmsort),
[docs.rs](https://docs.rs/llmsort)), the research code (its
`experiments/` workspace crate), and the full measured record (its
`research/` directory: replayable evidence packs, dated notes, campaign
definitions, python analysis, and `PROGRAM.md`), absorbed 2026-08-19 at
llmsort `28eb82c`.

This repo remains only as pre-extraction git history: the
`cardinal-harness` → `ratiometer` → `llmsorting` era, every version of
the in-tree crate, and the original commits behind each evidence pack.
Old links keep resolving via GitHub redirects.

MIT.
